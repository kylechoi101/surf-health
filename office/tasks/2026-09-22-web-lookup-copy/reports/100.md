## Done

Executed Phase 100 verification across the codebase:
- Verified that all allowed files in `app/`, `components/`, and `lib/` are completely free of the target phrases (`"modeled risk"`, `"ML model"`, `"machine learning models"`, `"beta model estimate"`, `"Model estimates"`).
- Ran type checking (`npx tsc --noEmit`), linting (`npm run lint`), and unit test suite (`npm test`). All 187 tests pass cleanly.

Acceptance command output (`npm test`):
```
✔ wetsuit ladder covers the full CA water-temp range (0.702208ms)
✔ recommendations get warmer monotonically as water cools (0.065625ms)
✔ band boundaries land on the chart's °F thresholds (0.047ms)
✔ missing or invalid water temp returns null, never a guess (0.086ms)
ℹ tests 187
ℹ suites 0
ℹ pass 187
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 421.87525
```

## Files changed
app/(desktop)/beaches/[id]/BeachDetail.tsx
app/(desktop)/methodology/page.tsx
app/(desktop)/research/calibration/page.tsx
app/(desktop)/research/labels/page.tsx
app/(desktop)/terms/page.tsx
components/map-section.tsx
components/poster-map.tsx
lib/forecastPresentation.ts
lib/riskData.ts
tests/forecast-presentation.test.mjs
tests/forecast-rollup.test.mjs
tests/risk-data.test.mjs

## Deviations
none

## Open questions
The global acceptance grep command:
`! grep -rn -e "modeled risk" -e "ML model" -e "machine learning models" -e "beta model estimate" -e "Model estimates" app components lib`
finds two matches in files that are not listed in `## Allowed files`:
1. `app/(desktop)/research/sources/page.tsx:38`: `"...multi-year historical sample archive that trains the ML model..."` (matches `"ML model"`).
2. `components/RiskComponents.tsx:160`: `/** Renders a compact "Beta forecast" pill when the forecast is a beta model estimate. */` (matches `"beta model estimate"`).

Per Worker Rule 4 ("Touch only files matching the brief's `## Allowed files`. If the phase cannot be completed without another file, do not touch it; explain under `Open questions` in your report and stop"), these files were left untouched. To make the grep command pass cleanly, the PM will need to add `app/(desktop)/research/sources/page.tsx` and `components/RiskComponents.tsx` to `## Allowed files` (or update `## Corrections`), permitting:
- In `app/(desktop)/research/sources/page.tsx:38`: changing `trains the ML model` to `trains the ML challenger` or similar.
- In `components/RiskComponents.tsx:160`: changing `beta model estimate` to `beta estimate` in the comment.

## Next
Task complete. Awaiting PM review and signoff or instructions on whether to permit edits to `app/(desktop)/research/sources/page.tsx` and `components/RiskComponents.tsx`.
