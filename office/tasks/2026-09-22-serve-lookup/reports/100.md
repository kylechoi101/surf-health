## Done

- Added the "Served estimate: per-beach lookup (2026-09-22)" section to `CLAUDE.md` detailing the formula (`p_lookup = (pos + 5*g) / (n + 5)` over trailing 365d), the persistence floor, the Beta credible interval, and the export-time advisory floor. Noted that the ML pipeline still trains and preserves `p_exceed_ml` and `risk_band_ml`, and documented the motivation (90d AUROC 0.878 vs 0.824, AUCPR 0.580 vs 0.395, Brier 0.060 vs 0.071 on forward 1–3 day lab outcomes).
- Ran all Acceptance commands against the `/tmp/lookup_accept` snapshot copy:
  - `risk_band` counts on the temp copy: `{'Low': 321, 'High': 41, 'Moderate': 33, 'Very High': 7}`
  - `validate_forecast.py` output: `PASS: 402 rows, forecast_date=2026-09-22, p_exceed range [0.0119, 0.9477] (218 distinct). exit=0`
  - `serving_snapshot` generated `/tmp/lookup_accept/serving.sqlite` (exit 0)
  - Unit tests: 8 passed in `tests/test_lookup_serving.py`
  - Linter: `ruff check` passed cleanly
  - Full backend test suite result: 670 passed, 0 failed

```
$ cd backend && .venv/bin/python -c "import pandas as pd; f=pd.read_parquet('/tmp/lookup_accept/forecasts.parquet'); p=pd.read_parquet('/tmp/lookup_prev.parquet'); assert (f.model_version=='lookup-365d-v1').all(); assert (f.p_exceed_ml.values==p.p_exceed.values).all(); assert len(f)==len(p); print(f.risk_band.value_counts().to_dict())"
{'Low': 321, 'High': 41, 'Moderate': 33, 'Very High': 7}
$ cd backend && SKIP_FORECAST_ANOMALY_CHECKS=0 .venv/bin/python scripts/validate_forecast.py --curated /tmp/lookup_accept --previous /tmp/lookup_prev.parquet ; echo "exit=$?"
PASS: 402 rows, forecast_date=2026-09-22, p_exceed range [0.0119, 0.9477] (218 distinct).
exit=0
$ cd backend && .venv/bin/python -m pytest -q -x -p no:cacheprovider 2>&1 | tail -3
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
670 passed, 9 warnings in 18.11s
$ cd backend && .venv/bin/ruff check app/ml/lookup_serving.py tests/test_lookup_serving.py app/ml/served_metrics.py
All checks passed!
```

## Files changed

CLAUDE.md

## Deviations

none

## Open questions

none

## Next

All phases (25, 50, 75, 100) are complete. Ready for PM review and commit/push.
