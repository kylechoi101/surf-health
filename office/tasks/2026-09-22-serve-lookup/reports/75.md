## Done

- Updated `.github/workflows/daily-forecast.yml`:
  - Added new step `Serve the lookup estimate` immediately after `Run ML training + forecasts` and before `Honor release gate for hourly forecast`, running `python -m app.ml.lookup_serving --curated ../data/curated/` in `working-directory: backend`.
  - Changed `--forecast-min-recency-days 45` to `30` in the `Run ML training + forecasts` step.
- Verified that `validate_forecast.py` and `serving_snapshot` Acceptance commands pass cleanly on the temporary copy (`/tmp/lookup_accept`):
  - `validate_forecast.py` passed with exit code 0 (`PASS: 402 rows, forecast_date=2026-09-22, p_exceed range [0.0119, 0.9477] (218 distinct). exit=0`).
  - `serving_snapshot` passed with exit code 0 and generated `/tmp/lookup_accept/serving.sqlite`.
  - All 8 unit tests in `tests/test_lookup_serving.py` passed.
  - Linter (`ruff check`) passed cleanly.

```
$ SKIP_FORECAST_ANOMALY_CHECKS=0 .venv/bin/python scripts/validate_forecast.py --curated /tmp/lookup_accept --previous /tmp/lookup_prev.parquet ; echo "exit=$?"
PASS: 402 rows, forecast_date=2026-09-22, p_exceed range [0.0119, 0.9477] (218 distinct).
exit=0
$ .venv/bin/python -m app.data.pipeline.serving_snapshot --curated /tmp/lookup_accept
/tmp/lookup_accept/serving.sqlite
$ .venv/bin/python -m pytest tests/test_lookup_serving.py -q
........                                                                 [100%]
8 passed in 1.04s
```

## Files changed

.github/workflows/daily-forecast.yml

## Deviations

none

## Open questions

none

## Next

Phase 100: Add the "Served estimate: per-beach lookup (2026-09-22)" section to `CLAUDE.md`, run the full backend test suite (`pytest -q -x -p no:cacheprovider`), execute all Acceptance commands, and report risk_band counts, validate_forecast output, and full-suite results in `reports/100.md`.
