## Done

- Implemented `compute_lookup(beach_day, forecast_date, beach_ids, window_days=365, prior_strength=5)` in `backend/app/ml/lookup_serving.py`:
  - Pure function filtering `beach_day` records strictly to `[forecast_date - 365d, forecast_date)`
  - Computes pooled exceedance rate `g` across all beaches in the window
  - Computes per-beach Empirical Bayes estimate `p_lookup = (pos + 5*g) / (n + 5)` (defaults to `g` when `n=0`)
  - Tracks `last_sample_date` and `last_exceeds` from the most recent sample strictly before `forecast_date`
  - Applies persistence floor to `_LOW_THRESHOLD` when `last_exceeds` is True
  - Computes Beta credible interval `p_exceed_lower` and `p_exceed_upper` (5th/95th percentiles) with bounds adjusted to bracket `base`
- Added tests in `backend/tests/test_lookup_serving.py`:
  - Test (1): strictly prior records evaluated; samples on or after `forecast_date` are ignored (`test_compute_lookup_strictly_prior_row_on_d_ignored`)
  - Test (2): zero-sample beach receives `p_lookup == g` with null `last_*` attributes (`test_compute_lookup_zero_sample_beach_gets_exactly_g`)
- Verified linter checks and ran Phase 25 tests:

```
$ cd backend && .venv/bin/python -m pytest tests/test_lookup_serving.py -q
..                                                                       [100%]
2 passed in 0.91s

$ cd backend && .venv/bin/ruff check app/ml/lookup_serving.py tests/test_lookup_serving.py
All checks passed!
```

## Files changed

backend/app/ml/lookup_serving.py
backend/tests/test_lookup_serving.py

## Deviations

none

## Open questions

none

## Next

Phase 50: Implement `apply_lookup_to_served` (steps a–f) and CLI entrypoint in `backend/app/ml/lookup_serving.py`, update `backend/app/ml/served_metrics.py`, add tests (3)–(8) in `backend/tests/test_lookup_serving.py`, and run the first three Acceptance commands.
