## Done

- Implemented `apply_lookup_to_served(curated_dir: Path)` and CLI entrypoint in `backend/app/ml/lookup_serving.py`:
  - (a) Idempotently copies `p_exceed` to `p_exceed_ml` and `risk_band` to `risk_band_ml` when `model_version != LOOKUP_MODEL_VERSION`.
  - (b) Writes lookup estimates into `p_exceed`, `p_exceed_raw`, `p_exceed_lower`, `p_exceed_upper`, `risk_band`, `persistence_floor_applied`, and `advisory_floor_applied` (advisory floor applied to `_HIGH_THRESHOLD` if active in `advisories.parquet` with `started_at <= D`).
  - Sets `model_version = "lookup-365d-v1"`, resets ML-regressor outputs and `served_offset_weight` to null/NaN, generates ordered plain-English `top_drivers` with Mon D date formatting.
  - Leaves `p_exceed_precal`, `sample_age_days`, `sample_recency_band`, `forecast_label_mode`, `forecast_generated_at`, and environmental columns untouched.
  - (c) Updates `forecast_history.parquet`: updates matched today rows with served fields and preserves `p_exceed_ml`; backfills `p_exceed_ml = p_exceed` for older history rows where `p_exceed_ml` was null.
  - (d) Added `"p_exceed_ml"` to `_HISTORY_COLUMNS` and `_PROBABILITY_COLUMNS` in `backend/app/ml/served_metrics.py`.
  - (e) Writes `system_health.json["serving_method"]` with execution metadata using `dumps_strict`.
  - (f) Performs all parquet writes atomically via `.tmp` file and `os.replace`.
- Implemented tests (3) through (8) in `backend/tests/test_lookup_serving.py`:
  - Test (3): persistence floor binds and sets flag
  - Test (4): posted beach floored to `_HIGH_THRESHOLD` with `p_exceed_raw` unfloored
  - Test (5): credible interval bounds `p_exceed_raw` (`lower <= p_exceed_raw <= upper`)
  - Test (6): idempotency preserves `p_exceed_ml` on repeated runs
  - Test (7): history rows for today updated, older rows backfilled
  - Test (8): `_HISTORY_COLUMNS` and `_PROBABILITY_COLUMNS` contain `p_exceed_ml`
- Executed the first three acceptance commands and test suite:

```
$ .venv/bin/python -m app.ml.lookup_serving --curated /tmp/lookup_accept
{
  "serving_method": "lookup-365d-v1",
  "forecast_date": "2026-09-22",
  "rows": 402,
  "posted": 14,
  "persistence_floored": 5,
  "advisory_floored": 12,
  "history_updated": 402,
  "applied_at": "2026-09-23T00:29:26.656299+00:00"
}
$ .venv/bin/python -c "import pandas as pd; f=pd.read_parquet('/tmp/lookup_accept/forecasts.parquet'); p=pd.read_parquet('/tmp/lookup_prev.parquet'); assert (f.model_version=='lookup-365d-v1').all(); assert (f.p_exceed_ml.values==p.p_exceed.values).all(); assert len(f)==len(p); print(f.risk_band.value_counts().to_dict())"
{'Low': 321, 'High': 41, 'Moderate': 33, 'Very High': 7}
$ .venv/bin/pytest tests/test_lookup_serving.py -q
........                                                                 [100%]
8 passed in 1.01s
```

## Files changed

backend/app/ml/lookup_serving.py
backend/app/ml/served_metrics.py
backend/tests/test_lookup_serving.py

## Deviations

none

## Open questions

none

## Next

Phase 75: Add workflow step in `.github/workflows/daily-forecast.yml` (`Serve the lookup estimate` after `Run ML training + forecasts` and before `Honor release gate for hourly forecast`), update `--forecast-min-recency-days` from 45 to 30, and verify `validate_forecast.py` and `serving_snapshot` acceptance commands pass on the temp copy.
