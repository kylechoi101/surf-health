## Done
Updated `backend/scripts/lookup_baseline.py` and `backend/tests/test_lookup_baseline.py` to implement the Phase 50 corrections and complete forecast mode end to end on snapshot `data/snapshots/main-2026-09-22`.
- Updated `advisory_active` condition per spec correction: active on D requires `started_at <= D` and either `ended_at >= D` or (`ended_at` null and `status == "active"`). On 2026-09-22, this identifies 23 active beaches (down from 139 false positives caused by historical advisories with null `ended_at`).
- Added `n_365d` sample counts alongside `rate_365d` and shrunk `p_lookup`.
- Successfully generated `data/experiments/lookup_baseline/lookup_forecast_2026-09-22.parquet` containing 850 rows (all beaches in `beaches.parquet`) with all required columns and precedence bands.

Acceptance output:
```
backend/.venv/bin/python -m pytest backend/tests/test_lookup_baseline.py -q
.......                                                                  [100%]
7 passed in 0.04s

backend/.venv/bin/python backend/scripts/lookup_baseline.py --snapshot data/snapshots/main-2026-09-22 --out data/experiments/lookup_baseline --forecast-date 2026-09-22
Wrote 850 rows to data/experiments/lookup_baseline/lookup_forecast_2026-09-22.parquet

test -s data/experiments/lookup_baseline/lookup_forecast_2026-09-22.parquet

cd backend && .venv/bin/ruff check scripts/lookup_baseline.py tests/test_lookup_baseline.py
All checks passed!
```

## Files changed
backend/scripts/lookup_baseline.py
backend/tests/test_lookup_baseline.py

## Deviations
none

## Open questions
none

## Next
Phase 75: Implement comparison reporting (`comparison_2026-09-22.md` + `.csv`) and backtest mode (`backtest.md` + `backtest_by_beach.csv`).
