# Model Card: Shorelife `xgb-undersample-ensemble-curated-v0`

## Deployment Status
- **Generated at**: 2026-06-11T05:58:48.019392+00:00
- **Deployment stage**: candidate_ready
- **Public release eligible**: true
- **Promotion blocker (latest)**: None

## Headline Metrics (from `system_health.json`)

### Temporal (held-out time slice)
- **AUCPR**: 0.751
- **Brier**: 0.096
- **Log loss**: 0.320
- **Calibration slope**: 1.152

### Deployment (active stations only; recency-filtered roster)
- **AUCPR**: —
- **Brier**: —
- **n_samples**: 0

### Validation (calibration/training-time slice; not a public headline)
- **AUCPR**: 0.693
- **Brier**: 0.077

### Spatial (holdouts)
- **Spatial county AUCPR**: 0.566
- **Spatial county persistence AUCPR**: 0.420

## Operational Agreement Check
Active advisories are decomposed into three pools by age. The overall agreement rate below is dominated by the stale pool (administrative postings the model is not designed to flag), so per-pool numbers are the honest model-quality signal.

- **Acute** (started ≤14 d, real outbreaks): 0 advised → agreement —
- **Chronic** (15-365 d, geomean postings): agreement —
- **Stale** (>365 d, admin zombies the model is not expected to flag): agreement —

- **Active-advisory agreement rate** (legacy overall metric, dominated by stale pool): 0.800

## Notes
- Forecasts are decision support and are not official lab results.
- Active official advisories override displayed risk in consumer surfaces.

## Metrics reconciliation (2026-06-10)
> This card is regenerated by `training.py`; this section may be overwritten on the
> next run. Durable record: `backend/METRICS_RECONCILIATION.md`.

- The headline **spatial county AUCPR 0.499 / beach 0.871** (calib slopes 0.99 / 0.88,
  county persistence baseline 0.370, pooled county base rate 0.175) are the **honest,
  shipped held-out generalization numbers** — the in-pipeline leave-one-group-out gate.
- A prior **0.590 county / 0.900 beach** figure (slopes 1.26/1.16) circulated in docs;
  it came from the offline `scripts/spatial_compare.py` (no isotonic calibration on the
  held-out predictions, no inner train/valid split, counties chosen by positive count)
  and was **never reproduced by the gate that ships**. Do not cite it. The model is
  identical in both paths — only the eval methodology differs.
- **Searcy et al. 2018 (median sens 0.50 @ spec 0.87)**: our "sens 0.59 @ spec 0.87"
  comparison is **UNVERIFIED** — no holdout prediction artifact is persisted, so it
  cannot be recomputed without a retrain. A reproducible `sensitivity_at_specificity()`
  helper now exists in `app/ml/evaluation.py`; persist the gate/test (label, prob) rows
  to verify it.
