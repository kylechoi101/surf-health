services:
  # The FastAPI Backend + Background ML Pipeline
  - type: web
    name: surf-health-api
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./
    plan: free
    envVars:
      - key: APP_ENV
        value: production
